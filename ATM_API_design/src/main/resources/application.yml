spring:
  datasource:
    url: jdbc:mysql://localhost:3306/atm_db?useSSL=false&serverTimezone=Asia/Seoul
    username: root
    password: 12300
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
